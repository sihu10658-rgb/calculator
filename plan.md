
# [개발계획서] 별난 계산기 프로젝트 (Crazy Calculator)

## 1. 프로젝트 개요
* **프로젝트명:** 별난 계산기 (Crazy Calculator)
* **목표:** 표준적인 사칙연산부터 고난도 수학 함수(제타 함수, 로그, 지수) 및 난수, 소수 관련 기능, 그리고 함수 그래프 시각화 기능까지 탑재한 웹 기반 공학용 계산기. 단, 사용자 인터페이스(UI)는 극도로 기괴하고 예측 불가능한 '이상한 유저 인터페이스'로 구현하여 유머러스한 사용자 경험 제공.
* **사용 기술:** Vanilla JavaScript, HTML5, CSS3 (외부 프레임워크 없이 순수 JS 구현)

---

## 2. 주요 기능 요구사항
1. **기본 사칙연산:** 덧셈(+), 뺄셈(-), 곱셈(*), 나눗셈(/)
2. **로그 연산:** 상용로그($\log_{10}$), 자연로그($\ln$) 등
3. **지수 함수:** 거듭제곱($x^y$), 지수함수($e^x$)
4. **랜덤 함수:** 지정 범위 내 난수 생성 (Random)
5. **소수(Prime) 계량 함수:** 소수 판별, 특정 수 이하의 소수 개수 산출($\pi(x)$) 등
6. **제타 함수:** 리만 제타 함수($\zeta(s)$) 계산 기능 (근사치 연산 또는 주요 특수값 처리)
7. **그래프 시각화:** 입력된 수식 또는 함수의 2D 그래프 렌더링 (HTML5 Canvas 활용)

---

## 3. UI/UX 컨셉: "이상한 유저 인터페이스 (Weird UI)"
* **비선형적 버튼 배치:** 숫자들이 뒤죽박죽 섞여 있거나, 클릭할 때마다 위치가 랜덤하게 살짝 움직이는 버튼.
* **불안정한 레이아웃:** 찌그러진 입력창, 마우스 오버 시 도망가는 버튼, 크기가 제각각인 폰트.
* **반응형 트롤링:** 계산 버튼을 누를 때마다 엉뚱한 사운드/애니메이션 효과나 방해 팝업(선택적) 출력.

---

## 4. 시스템 아키텍처 및 모듈 구조


```

calculator-project/
│
├── index.html       # 메인 UI 구조 (기괴한 레이아웃 포함)
├── style.css        # 이상한 UI를 위한 커스텀 스타일 및 애니메이션
└── script.js        # 수학 연산 로직 + 그래프 렌더링 + UI 트롤링 제어

```

---

## 5. 개발 단계별 일정 (로드맵)

### [1단계] 기획 및 설계 (1~2일차)
* 기괴한 UI 레이아웃 와이어프레임 스케치
* 수학 함수 연산 알고리즘 설계 (특히 제타 함수 및 소수 연산 최적화)

### [2단계] 수학 연산 및 코어 로직 구현 (3~4일차)
* `script.js` 내부에 수학 연산 라이브러리 구축
  * 사칙연산 및 지수/로그 함수
  * 소수 판별 및 소수 계량 함수 구현
  * 리만 제타 함수 근사 연산 구현
  * 난수 생성 함수 구현

### [3단계] 그래프 시각화 모듈 구현 (5일차)
* HTML5 Canvas를 활용한 좌표평면 드로잉 함수 작성
* 사용자가 입력한 함수식을 파싱하여 캔버스 위에 그래프 곡선으로 렌더링

### [4단계] 이상한 UI(Weird UI) 적용 및 인터랙션 구현 (6일차)
* CSS를 이용해 버튼 배열을 뒤틀고 시각적 혼란 유도
* JS를 통해 버튼 클릭 시 움직이거나 텍스트가 깨지는 등 트롤링 효과 부여

### [5단계] 통합 테스트 및 디버깅 (7일차)
* 예외 처리(0으로 나누기, 음수 로그 등) 확인
* UI가 과도하게 사용을 방해하지 않는 선에서 유머러스함 조율 및 최종 배포
